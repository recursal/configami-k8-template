//
// Default input settings to use
//
{
	// Namespace to use
	//-------------------------------------------
	"namespace" : "hello-namespace",
	
	// Deployment name to use
	//-------------------------------------------
	"name" : "hello-world",

	// Encode data with base64,
	// disable if ur input data is already encoded
	//-------------------------------------------
	"encode_base64": true,

	// Data mapping to convert
	//-------------------------------------------
	// "data" : {}

	// Payload files read raw + base64-encoded like "data" values;
	// map data key -> file path, relative to "planDir" (forward it
	// from the plan as "planDir": "{{planDir}}") or absolute
	//-------------------------------------------
	// "fileContent" : { "wg0.conf": "wireguard.conf" }

}